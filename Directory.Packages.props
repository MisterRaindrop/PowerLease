<Project>
  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    <!-- 需要它才能把传递依赖钉到安全版本，见下面 SQLitePCLRaw.lib.e_sqlite3 -->
    <CentralPackageTransitivePinningEnabled>true</CentralPackageTransitivePinningEnabled>
  </PropertyGroup>
  <ItemGroup>
    <PackageVersion Include="xunit.v3" Version="3.2.2" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="3.1.5" />
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="18.8.1" />
    <PackageVersion Include="coverlet.collector" Version="10.0.1" />
    <PackageVersion Include="Microsoft.Extensions.Hosting" Version="10.0.10" />
    <PackageVersion Include="Microsoft.Extensions.Hosting.WindowsServices" Version="10.0.10" />
    <PackageVersion Include="Microsoft.Data.Sqlite" Version="10.0.10" />
    <PackageVersion Include="System.CommandLine" Version="2.0.10" />
  </ItemGroup>
  <ItemGroup>
    <!--
      安全性传递固定，不是随意升级：
      Microsoft.Data.Sqlite 10.0.10（当前最新）会拉入 SQLitePCLRaw.lib.e_sqlite3 2.1.11，
      该版本存在已知高危漏洞 GHSA-2m69-gcr7-jv3q，配合仓库的 TreatWarningsAsErrors
      会让 NU1903 变成 restore 错误。2.1.12 已修复。
      降级或改用 <NoWarn>NU1903</NoWarn> / <NuGetAudit>false</NuGetAudit> 都是掩盖漏洞 ——
      这是个以 SYSTEM 权限运行的 Windows Service，不得这么做。
      等 Microsoft.Data.Sqlite 自己升到已修补版本后，本条可以移除。
    -->
    <PackageVersion Include="SQLitePCLRaw.lib.e_sqlite3" Version="2.1.12" />
  </ItemGroup>
</Project>
